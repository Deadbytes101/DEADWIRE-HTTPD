# V1.3 KEEP-ALIVE NATIVE BENCH

Branch: `feat/v1.3-keepalive`

Server binary:

```txt
build/deadwire_keepalive_experimental.exe
```

Client:

```txt
tools/deadwire_bench.c --keepalive
```

Mode shape:

```txt
mode=close      one request per TCP connection
mode=keepalive  one TCP connection per round, many sequential requests on that socket
```

This is the first apples-to-apples client comparison path because the close and keep-alive modes use the same native C benchmark client.

## Command shape

Manual shape:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build-win-keepalive-experimental.ps1
$exe = (Resolve-Path .\build\deadwire_keepalive_experimental.exe).Path
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/bench-native.ps1 -ServerExePath $exe -Port 19850 -Path /health -Requests 32768 -Rounds 5 -KeepAlive
```

Make target shape:

```powershell
make build-keepalive-experimental
make bench-native-keepalive-experimental
```

## First observed median results

```txt
/health              median_rps=8437.33 median_avg_ms=0.119 bytes=3833856
/hello.txt           median_rps=5685.94 median_avg_ms=0.176 bytes=4096000
/missing-bench.txt   median_rps=6584.26 median_avg_ms=0.152 bytes=4096000
/                    median_rps=6194.62 median_avg_ms=0.161 bytes=46235648
```

## Make target observed median results

```txt
/health              median_rps=8349.77 median_avg_ms=0.120 bytes=3833856
/hello.txt           median_rps=4774.86 median_avg_ms=0.209 bytes=4096000
/missing-bench.txt   median_rps=6140.67 median_avg_ms=0.163 bytes=4096000
/                    median_rps=5373.76 median_avg_ms=0.186 bytes=46235648
```

## Same-session quiet vs keep-alive comparison

This run executed the guardrail and both native benchmark targets in the same shell session:

```powershell
make verify
make verify-quiet
make probe-keepalive
make bench-native-quiet
make bench-native-keepalive-experimental
```

Guardrail result:

```txt
make verify: ok
make verify-quiet: ok
make probe-keepalive: close-after-response confirmed path=/health first_bytes=112
```

Quiet close-after-response median results:

```txt
/health              median_rps=7186.41 median_avg_ms=0.139 bytes=3670016
/missing-bench.txt   median_rps=6483.34 median_avg_ms=0.154 bytes=3932160
/hello.txt           median_rps=4862.58 median_avg_ms=0.206 bytes=3932160
/                    median_rps=4995.46 median_avg_ms=0.200 bytes=46071808
```

Experimental keep-alive median results:

```txt
/health              median_rps=8968.87 median_avg_ms=0.111 bytes=3833856
/hello.txt           median_rps=6282.09 median_avg_ms=0.159 bytes=4096000
/missing-bench.txt   median_rps=7734.97 median_avg_ms=0.129 bytes=4096000
/                    median_rps=6233.31 median_avg_ms=0.160 bytes=46235648
```

Same-session keep-alive gain:

```txt
/health              rps +24.8% avg_ms -20.1%
/hello.txt           rps +29.2% avg_ms -22.8%
/missing-bench.txt   rps +19.3% avg_ms -16.2%
/                    rps +24.8% avg_ms -20.0%
```

## Comparison against V1.2 quiet native baseline

Baseline used here is the final clean V1.2 quiet run:

```txt
/health              median_rps=6570.15 median_avg_ms=0.152
/missing-bench.txt   median_rps=5700.92 median_avg_ms=0.175
/hello.txt           median_rps=5106.36 median_avg_ms=0.196
/                    median_rps=5147.70 median_avg_ms=0.194
```

Estimated keep-alive gain from the first run:

```txt
/health              rps +28.4% avg_ms -21.7%
/hello.txt           rps +11.4% avg_ms -10.2%
/missing-bench.txt   rps +15.5% avg_ms -13.1%
/                    rps +20.3% avg_ms -17.0%
```

Estimated keep-alive gain from the make target run:

```txt
/health              rps +27.1% avg_ms -21.1%
/hello.txt           rps -6.5%  avg_ms +6.6%
/missing-bench.txt   rps +7.7%  avg_ms -6.9%
/                    rps +4.4%  avg_ms -4.1%
```

## Interpretation

```txt
native keep-alive clearly wins for /health across repeated runs
the same-session comparison shows keep-alive winning on all tested paths
this is still not a concurrency win; it is a connection-reuse win
same-session results are stronger than cross-session comparisons because machine load and benchmark state are closer
```

## Guardrail

The default binary should remain close-after-response until this experimental path is promoted deliberately.

Recommended checks before promotion:

```powershell
make verify
make verify-quiet
make probe-keepalive
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/probe-keepalive-experimental-matrix.ps1
make bench-native-quiet
make bench-native-keepalive-experimental
```
